config BR2_PACKAGE_READ_KEY
	bool "read-key"
	depends on BR2_TOOLCHAIN_HAS_THREADS
	help
	  Wait for any key press, print the keycode and exit

comment "read-key needs a toolchain w/ threads"
	depends on !BR2_TOOLCHAIN_HAS_THREADS
